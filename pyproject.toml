[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"

[tool.hatch.build.targets.wheel]
packages = ["RoutingGraphEnv"]

[project]
name = "RoutingGraphEnv"
version = "0.0.1"
dependencies = [
  "gymnasium",
  "osmnx",
  "pygame>=2.1.3",
]
